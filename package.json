{
  "name": "tsc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "ts"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clear && run compy-index-html && npm run compy-javascript && tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^9.3.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "npm-watch": "^0.13.0"
  }
}
